sources = ['IPlugEffect.cpp']
incdirs = include_directories('.')
# app_lib = static_library('app',
#                         sources,
#                         include_directories : incdirs)

app_dep = declare_dependency(
  include_directories: incdirs,
  sources: sources
  )
